Options -Indexes
RewriteEngine On

# Forcer HTTPS + version préférée (sans www)
RewriteCond %{HTTPS} off [OR]
RewriteCond %{HTTP:X-Forwarded-Proto} !https
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]

# Autoriser les fichiers statiques réels
RewriteCond %{REQUEST_FILENAME} !-f

# Exclure le dossier admin (géré par son propre .htaccess)
RewriteCond %{REQUEST_URI} !^/admin/

# Tout passer à index.php (y compris les dossiers comme /blog/, /actualites/, etc.)
RewriteRule ^ index.php [QSA,L]

# Sécurité : cacher les fichiers sensibles
<FilesMatch "\.(env|log|sql|git|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Headers de sécurité
<IfModule mod_headers.c>
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# Cache statique
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/webp       "access plus 1 year"
    ExpiresByType image/jpeg       "access plus 1 year"
    ExpiresByType image/png        "access plus 1 year"
    ExpiresByType text/css         "access plus 1 month"
    ExpiresByType text/javascript  "access plus 1 month"
</IfModule>


# Gestion des erreurs
ErrorDocument 404 /index.php
